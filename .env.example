# Contact Form API (Cloudflare Worker proxy)
# Endpoint público del Worker que recibe el formulario y lo reenvía a n8n.
VITE_CONTACT_API_URL=https://contact-proxy.manta.com.ar/api/contact

# --- Server-side only (NO prefix VITE_) ---
# N8N_WEBHOOK_URL=https://n8n.manta.com.ar/webhook/contact-form
